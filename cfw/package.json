{
	"name": "cfw-tebiane",
	"version": "0.0.4",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.7.5",
		"wrangler": "^4.9.1"
	},
	"dependencies": {
		"cheerio": "^1.0.0"
	}
}
