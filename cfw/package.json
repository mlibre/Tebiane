{
	"name": "cfw-tebiane",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --local",
		"start": "wrangler dev --local",
		"build-quran": "node scripts/merging_quran_translations.js"
	},
	"dependencies": {
		"fuse.js": "^7.1.0",
		"lodash": "^4.17.21"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.7.5",
		"eslint": "^9.4.0",
		"wrangler": "^4.9.1"
	}
}